## Estrutura recomendada

**1. Título e descrição** — explique brevemente o que a API faz.

**2. Tecnologias utilizadas**
- Java 17+
- Spring Boot 3.x
- Spring Web
- Spring Data JPA
- PostgreSQL
- Docker

**3. Pré-requisitos**
- Java 17+
- Docker
- Git

**4. Como rodar localmente**
```bash
git clone https://github.com/seu-usuario/seu-repositorio.git
cd seu-repositorio
docker-compose up -d
```

**5. Endpoints principais** — liste as rotas da API com método e descrição.

**6. Variáveis de ambiente** — documente as configs necessárias com um `.example`.

**7. Autor** — nome e links para LinkedIn e GitHub.